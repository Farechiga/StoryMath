# ── Dependencies ──────────────────────────────────────────────
node_modules/

# ── Build output ──────────────────────────────────────────────
dist/
*.tsbuildinfo

# ── OS / editor junk ──────────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# ── Internal process / dev artifacts (not needed to build or run) ─
# Design specs, audits, authoring/process docs, and Claude tooling — kept
# locally, excluded from the published repo.
audit/
docs/
prompts/
ClaudeCodePrompts/
CLAUDE.md
IMPLEMENTATION_NOTES.md
STORYMATH_PRODUCT_AND_BUILD_SPEC.md
.claude/

# ── Redundant / stray local files ─────────────────────────────
# The runnable app bundles its own copies of the art under
# storymath-change-engine-starter/src/assets/. This top-level folder is the
# original source art and is kept out of the repo to avoid ~50 MB of duplication.
/assets/
# A stray working screenshot and a stale duplicate problem pack (the canonical
# copy lives in storymath-change-engine-starter/data/problems/).
/image.png
/nasa-perseverance-wheel-slip.json
